export const metadata = {
    title: 'Liste des vehicules',
    description: 'liste de vehicules.',
  };
  
  export default function HomeLayout({ children }: { children: React.ReactNode }) {
    return <div>{children}</div>; 
  }